---
- name: Test ping-ping
  hosts: all
  become: yes
  gather_facts: yes
  tasks:
  - name: Ping my servers
    ping: 
  - name: print
    ansible.builtin.debug:
      msg: this is the value {{ hostvars['awxsetup.omid.expertcity.com'] }}
  - name: print group_vars
    ansible.builtin.setup:
      filter:
        - 'booter'
