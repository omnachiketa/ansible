---
- name: Test ping-ping
  hosts: all
  become: yes
  gather_facts: yes
  tasks:
  - name: Ping my servers
    ping: 
  - name: print
    ansible.builtin.debug:
      msg: this is the value {{ hostvars['awxsetup.omid.expertcity.com'] }}
  - name: read vars
    set_fact:
      list2: "{{ ansible_play_hosts_all|map('extract', hostvars, 'ansible_host')| join(',') }}
  - name: Print vars
    ansible.builtin.debug:
      var: list2
  - name: Print all available facts
    ansible.builtin.debug:
      var: ansible_facts
